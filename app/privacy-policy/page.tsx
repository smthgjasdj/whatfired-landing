import { SiteFrame, SectionLabel } from '@/components/site-shell'

export default function PrivacyPolicyPage() { return <SiteFrame><main className="subpage"><div className="container narrow policy"><SectionLabel>Legal</SectionLabel><h1>Privacy <em>policy.</em></h1><p className="subpage-intro">Whatfired records what your Meta Pixel sends so you can inspect it — and everything it records stays inside your browser. Last updated: September 14, 2026.</p><div className="content-block"><h2>Summary</h2><p>Everything Whatfired records is stored on your device only. Values that look personal are masked the moment they are read, recordings are kept for 15, 30 or 60 minutes and then discarded, and nothing is sold or shared with anyone — including Meta, ad networks, and data brokers. There are no accounts, no analytics, no telemetry and no advertising.</p></div><div className="content-block"><h2>What Whatfired observes</h2><p>On the pages you visit, the extension watches: calls the page makes to its own pixel function (fbq), requests the page sends to Meta&apos;s collection endpoint and the parameters attached to them, which pixel IDs are present and which frame each event came from, and the address and title of the tab being inspected so recordings can be grouped by page. It observes requests to Meta&apos;s endpoint only — traffic to any other destination passes through untouched. It does not read page text, form fields, images, cookies, local storage or the DOM beyond the pixel&apos;s own calls.</p></div><div className="content-block"><h2>Personal data and masking</h2><p>One kind of problem Whatfired exists to catch is a site sending personal data — an email, a phone number, a name — to Meta without hashing it first. To make that safe to inspect, values that look personal are masked at the moment they are read: an email becomes j***@e***.com, a phone number becomes +4*******58. The original text is never written to storage and never shown in the panel, so a screenshot of a debugging session is not itself a data breach. Exports contain masked values only, because no unmasked copy exists anywhere.</p></div><div className="content-block"><h2>Where the data lives</h2><p>Event recordings are held in the browser&apos;s session storage, keyed by tab, and are discarded when the retention window elapses or the tab closes. Your settings — language, theme, retention window, whether recording is paused — are held in the browser&apos;s local storage and kept until you change them or uninstall. There is no account, no sign-in, and no copy of your recordings outside this browser.</p></div><div className="content-block"><h2>Retention and deletion</h2><p>Recordings are kept for the window you choose — 15, 30 or 60 minutes (30 by default) — then deleted automatically. Closing a tab erases that tab&apos;s recording immediately. Uninstalling the extension removes all stored data with it.</p></div><div className="content-block"><h2>Network activity</h2><p>Your recorded event data never leaves your browser. The network traffic you see while using Whatfired is the traffic the pages you visit already produce on their own — the extension watches it happen and does not generate, forward or modify it. The extension contacts exactly one website of its own: the Whatfired product site, which hosts the release notes it opens after updates. That site keeps a record of the installed version so the notes match what you are running, and nothing else.</p></div><div className="content-block"><h2>Sharing</h2><p>Data recorded by the extension is never sold, rented or traded, and never shared with third parties — including Meta / Facebook, advertising networks, analytics providers and data brokers.</p></div><div className="content-block"><h2>Exports</h2><p>When you export a recording, the file is written by the popup from its own data on your device and contains masked values only.</p></div><div className="content-block"><h2>Permissions and why they are needed</h2><p><b>storage</b> — holds recorded events (session) and your settings (local) on your device; nothing recorded is ever written to a server. <b>alarms</b> — once a minute, wakes the extension to enforce the retention window: it deletes events older than your limit and discards recordings of tabs you have closed. <b>Host access to one site</b> — the extension&apos;s own product website only, which serves its release notes; no other site is accessed. <b>Content scripts</b> — two small scripts run on the pages you inspect, one in the page&apos;s own context to watch the pixel&apos;s fbq function and network calls, one in an isolated context to relay what it saw; they also run inside embedded frames, including frames with no URL of their own, because checkout and booking widgets frequently carry their own pixel. Every wrapped call is passed through to the original untouched — the page&apos;s tracking behaves exactly as it would without the extension.</p></div><div className="content-block"><h2>Children</h2><p>Whatfired is a developer tool and is not directed at children. It collects no personal information from anyone.</p></div><div className="content-block"><h2>Changes to this policy</h2><p>If this policy changes, the updated version is published on this page with a new date at the top. Continuing to use the extension after a change means you accept the revised policy.</p></div></div></main></SiteFrame> }
